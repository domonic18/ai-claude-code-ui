/**
 * Terminal Components
 *
 * Re-exports all terminal components.
 */

// Terminal component (exported as Shell and Terminal for backward compatibility)
export { Shell } from './Terminal';
export { Terminal } from './Terminal';

// StandaloneShell component
export { default as StandaloneShell } from './StandaloneShell';

# 代码分支管理规范

## 文档信息

| 项目 | 内容 |
|------|------|
| 文档版本 | v1.0.0 |
| 创建日期 | 2025-01-18 |
| 适用范围 | Claude Code UI 项目 |
| 团队规模 | 2-3人 |

---

## 1. 项目概述

### 1.1 项目结构

Claude Code UI 是一个为 Claude Code CLI、Cursor CLI 和 OpenAI Codex 提供多用户 Web 界面的全栈应用：

```
ai-claude-code-ui/
├── frontend/           # 前端（React + Vite）
├── backend/            # 后端（Express.js + WebSocket）
├── shared/             # 共享代码和类型定义
├── docs/               # 项目文档
├── docker/             # Docker 配置
└── scripts/            # 构建和部署脚本
```

### 1.2 技术栈

- **前端**：React 18 + Vite + Tailwind CSS + i18next
- **后端**：Express.js + WebSocket (ws) + SQLite
- **AI 集成**：@anthropic-ai/claude-agent-sdk、OpenAI Codex SDK
- **开发模式**：前后端分离，实时通信（WebSocket）

---

## 2. 分支管理模型

本项目采用 **简化的 Git Flow** 模型，针对 2-3 人小团队优化。

### 2.1 分支结构

```
main (主分支 - 生产环境，锁定)
  ├──
  └── develop (开发分支 - 日常开发)
        ├── feature/* (功能分支 - 按需创建)
        ├── refactor/* (重构分支 - 按需创建)
        ├── fix/* (修复分支 - 按需创建)
        └── hotfix/* (紧急修复 - 从 main 拉出)
```

### 2.2 分支定义

| 分支 | 命名规范 | 来源 | 目标 | 保护规则 |
|------|----------|------|------|----------|
| **main** | - | develop/PR | 生产部署 | 🔒 只读，强制 PR |
| **develop** | - | main/init | main/PR | 🔒 需要审核 |
| **feature** | feature/xxx | develop | develop/PR | ✅ 可直接推送 |
| **refactor** | refactor/xxx | develop | develop/PR | ✅ 可直接推送 |
| **fix** | fix/xxx | develop | develop/PR | ✅ 可直接推送 |
| **hotfix** | hotfix/xxx | main | main + develop | 🔒 需要审核 |

---

## 3. 分支职责详解

### 3.1 main 分支

**职责**：生产环境分支，始终保持稳定可部署状态

**规则**：
- ✅ 接受来自 develop 的 PR 合并
- ✅ 接受来自 hotfix 的 PR 合并
- ❌ 禁止直接提交
- ❌ 禁止直接推送
- 🔒 设置为保护分支
- 🏷️ 每次合并打 tag：`vX.Y.Z`

**示例**：
```bash
# 合并 develop 到 main（通过 PR）
# 在 GitHub 上创建 PR：develop -> main
# PR 审核通过后合并

# 打版本标签
git checkout main
git pull origin main
git tag -a v1.2.0 -m "Release version 1.2.0"
git push origin main --tags
```

### 3.2 develop 分支

**职责**：日常开发主分支

**规则**：
- ✅ 接受来自 feature/refactor/fix 的合并
- ✅ 可直接推送（小团队简化）
- 🧪 开发测试环境使用此分支
- 📦 定期合并到 main（通过 PR）

**使用场景**：
- 日常开发和测试
- 功能集成验证
- 代码审查前测试

### 3.3 feature/* 分支

**职责**：新功能开发分支

**何时创建**：
- 涉及多个文件的功能开发
- 需要长时间开发的功能（> 1天）
- 需要独立测试的功能

**命名规范**：
```
feature/<模块>-<功能描述>
feature/chat-slash-commands       # 聊天斜杠命令功能
feature/user-authentication      # 用户认证功能
feature/file-browser              # 文件浏览器功能
```

**生命周期**：
```bash
# 1. 从 develop 创建
git checkout develop
git checkout -b feature/chat-slash-commands

# 2. 开发和提交
git add .
git commit -m "feat: add slash command parser"

# 3. 完成后合并到 develop
git checkout develop
git merge --no-ff feature/chat-slash-commands

# 4. 删除分支
git branch -d feature/chat-slash-commands
```

### 3.4 refactor/* 分支

**职责**：代码重构分支

**何时创建**：
- 大规模代码重构（> 5 个文件）
- 架构调整
- 性能优化重构
- 依赖升级

**命名规范**：
```
refactor/<模块>-<重构描述>
refactor/frontend-architecture    # 前端架构重构
refactor/backend-api              # 后端 API 重构
refactor/state-management         # 状态管理重构
```

**生命周期**：与 feature 分支相同

### 3.5 fix/* 分支

**职责**：非紧急 bug 修复分支

**何时创建**：
- 需要 2 小时以上修复的 bug
- 涉及多个文件的 bug 修复

**命名规范**：
```
fix/<模块>-<问题描述>
fix/session-timeout               # 会话超时修复
fix/file-upload-failure           # 文件上传失败修复
fix/auth-redirect                 # 认证重定向修复
```

### 3.6 hotfix/* 分支

**职责**：生产环境紧急修复分支

**何时创建**：
- 生产环境严重 bug
- 安全漏洞修复
- 数据损坏修复

**命名规范**：
```
hotfix/<问题描述>
hotfix/crash-on-startup           # 启动崩溃修复
hotfix-security-leak              # 安全漏洞修复
hotfix-data-corruption            # 数据损坏修复
```

**生命周期**：
```bash
# 1. 从 main 创建
git checkout main
git checkout -b hotfix/crash-on-startup

# 2. 修复并提交
git add .
git commit -m "fix: prevent crash on startup"

# 3. 合并到 main 和 develop
git checkout main
git merge --no-ff hotfix/crash-on-startup
git push origin main

git checkout develop
git merge --no-ff hotfix/crash-on-startup
git push origin develop

# 4. 删除分支
git branch -d hotfix/crash-on-startup
```

---

## 4. 工作流程

### 4.1 日常开发流程（小改动）

适用于单文件或简单改动：

```
develop → 直接开发 → 提交 → 推送
```

**示例**：
```bash
git checkout develop
git pull origin develop

# 直接开发
vim frontend/components/Header.tsx

# 提交
git add frontend/components/Header.tsx
git commit -m "fix: correct header alignment"
git push origin develop
```

### 4.2 功能开发流程（大改动）

适用于多文件或复杂功能：

```
develop → feature/xxx → 开发 → 测试 → PR 到 develop → 合并
```

**示例**：
```bash
# 1. 创建功能分支
git checkout develop
git checkout -b feature/user-avatar-upload

# 2. 开发和提交
git add .
git commit -m "feat: add user avatar upload"

# 3. 推送到远程
git push -u origin feature/user-avatar-upload

# 4. 创建 PR（feature/xxx -> develop）
# 5. 通过 PR 审核后合并
```

### 4.3 合并到主分支流程

```
develop → 测试 → 单测 → 静态扫描 → AI 评审 → PR 到 main → 合并
```

**检查清单**：
- [ ] 功能测试通过
- [ ] 单元测试通过 (`npm test`)
- [ ] 代码静态扫描通过 (`npm run lint`)
- [ ] AI 代码评审通过
- [ ] 文档更新完成
- [ ] 创建 PR 到 main

**示例**：
```bash
# 1. 确保 develop 是最新的
git checkout develop
git pull origin develop

# 2. 运行测试
npm test
npm run lint
npm run build

# 3. 创建 PR（develop -> main）
# 在 GitHub 上创建 Pull Request

# 4. 等待审核通过后合并
```

---

## 5. 代码质量门禁

### 5.1 合并到 main 前必须通过

1. **功能测试**
   - 手动测试所有相关功能
   - 确认没有回归问题

2. **单元测试**
   ```bash
   npm test
   ```
   - 所有测试用例通过
   - 覆盖率不低于 70%

3. **代码静态扫描**
   ```bash
   npm run lint
   ```
   - 无 ESLint 错误
   - 无 TypeScript 类型错误
   - 修复所有警告

4. **AI 代码评审**
   - 使用 `/code-review` 进行代码审查
   - 修复所有高危问题
   - 评估并处理中低危问题

### 5.2 CI/CD 检查（可选）

如果配置了 GitHub Actions：

```yaml
# .github/workflows/ci.yml
name: CI
on: [pull_request]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-node@v3
      - run: npm ci
      - run: npm test
      - run: npm run lint
      - run: npm run build
```

---

## 6. 提交规范

### 6.1 提交消息格式

采用 [Conventional Commits](https://www.conventionalcommits.org/) 规范：

```
<type>(<scope>): <subject>

<body>

<footer>
```

### 6.2 类型 (type)

| 类型 | 说明 | 示例 |
|------|------|------|
| `feat` | 新功能 | `feat(chat): add slash command support` |
| `fix` | Bug 修复 | `fix(auth): correct token expiration check` |
| `refactor` | 重构 | `refactor(frontend): simplify state management` |
| `docs` | 文档 | `docs: update API documentation` |
| `style` | 格式 | `style: fix indentation` |
| `test` | 测试 | `test: add unit tests for auth service` |
| `chore` | 构建/工具 | `chore: update dependencies` |

### 6.3 提交示例

```bash
# 简单提交
git commit -m "fix: correct session timeout handling"

# 详细提交
git commit -m "feat(chat): add file attachment support

- Allow users to attach files to messages
- Display file previews in chat interface
- Add file type validation

Closes #123"

# 重构提交
git commit -m "refactor(frontend): extract user menu component

- Move user menu from Sidebar to separate component
- Simplify sidebar structure
- Improve code organization"
```

---

## 7. Pull Request 规范

### 7.1 PR 标题格式

```
<type>: <简短描述>

feat: 添加用户头像上传功能
fix: 修复会话超时问题
refactor: 重构前端状态管理
```

### 7.2 PR 描述模板

```markdown
## 变更类型
- [ ] 新功能
- [ ] Bug 修复
- [ ] 重构
- [ ] 文档更新
- [ ] 性能优化

## 变更说明
<!-- 详细描述本次变更的内容 -->

## 测试情况
- [ ] 功能测试通过
- [ ] 单元测试通过
- [ ] 代码静态扫描通过
- [ ] AI 代码评审通过

## 相关 Issue
Closes #123

## 检查清单
- [ ] 代码符合项目规范
- [ ] 添加了必要的注释
- [ ] 更新了相关文档
- [ ] 没有引入新的警告

## 截图/演示
<!-- 如果有 UI 变更，请提供截图或演示 GIF -->
```

---

## 8. 分支保护规则

### 8.1 main 分支保护

在 GitHub 设置中配置：

| 保护项 | 规则 |
|--------|------|
| 🔒 推送保护 | 启用 |
| ✅ 审查要求 | 至少 1 人批准 |
| 🧪 状态检查 | CI 通过（如有） |
| 📊 限制合并 | 只有管理员可合并 |
| 🔄 合并前更新 | 必须更新到最新 |

### 8.2 develop 分支保护

| 保护项 | 规则 |
|--------|------|
| ✅ 审查要求 | 可选（小团队可省略） |
| 🧪 状态检查 | 可选 |

---

## 9. 版本管理规范

### 9.1 版本号规范

采用语义化版本 [Semantic Versioning 2.0.0](https://semver.org/)：

```
MAJOR.MINOR.PATCH

例：1.2.0
  MAJOR (1):   重大版本更新，不兼容的 API 变更
  MINOR (2):   功能更新，向后兼容
  PATCH (0):   Bug 修复，向后兼容
```

### 9.2 标签规范

```bash
v1.0.0    # 首个稳定版本
v1.1.0    # 新增功能
v1.1.1    # Bug 修复
v2.0.0    # 重大版本更新
```

### 9.3 版本发布检查清单

- [ ] 所有功能测试通过
- [ ] 单元测试通过
- [ ] 代码静态扫描通过
- [ ] AI 代码评审通过
- [ ] 文档更新完成
- [ ] CHANGELOG.md 更新
- [ ] 创建版本标签
- [ ] 推送到远程

---

## 10. 最佳实践

### 10.1 分支管理

✅ **推荐做法**：
- 小步快跑，频繁提交
- 保持分支短生命周期（< 1 周）
- 使用描述性的分支名称
- 合并后删除已完成的分支
- 使用 `--no-ff` 合并保留历史

❌ **避免做法**：
- 在 main 分支上直接开发
- 长期存在的功能分支
- 不清楚的分支命名
- 遗留的僵尸分支
- 大批量代码一次性提交

### 10.2 代码提交

✅ **推荐做法**：
- 原子提交：每次提交只做一件事
- 清晰的提交消息
- 及时推送代码
- 提交前检查代码格式

❌ **避免做法**：
- 混合多个不相关的改动
- 含糊的提交消息
- 长时间不推送代码
- 提交有语法错误的代码

### 10.3 团队协作

✅ **推荐做法**：
- 开始开发前沟通计划
- 使用不同的功能分支避免冲突
- 定期合并到 develop
- 及时进行代码审查
- 保持良好的提交历史

❌ **避免做法**：
- 重复开发同一功能
- 长时间不合并代码
- 忽略代码冲突
- 跳过代码审查

---

## 11. 常用命令速查

### 11.1 分支操作

```bash
# 创建功能分支
git checkout develop
git checkout -b feature/your-feature

# 合并功能分支到 develop
git checkout develop
git merge --no-ff feature/your-feature

# 删除本地分支
git branch -d feature/your-feature

# 删除远程分支
git push origin --delete feature/your-feature

# 查看所有分支
git branch -a
```

### 11.2 合并操作

```bash
# 更新本地分支
git pull origin develop

# 合并到 main（通过 PR 后）
git checkout main
git pull origin main

# 创建版本标签
git tag -a v1.2.0 -m "Release version 1.2.0"
git push origin v1.2.0
```

### 11.3 冲突处理

```bash
# 合并时出现冲突
git checkout develop
git merge feature/your-feature

# 解决冲突后
git add .
git commit -m "merge: resolve conflicts in feature/your-feature"
```

---

## 12. 紧急情况处理

### 12.1 生产环境紧急修复

```bash
# 1. 从 main 创建 hotfix 分支
git checkout main
git checkout -b hotfix/critical-bug

# 2. 快速修复
vim backend/api/controller.js
git add .
git commit -m "hotfix: prevent crash on null pointer"

# 3. 合并到 main（快速通道）
git checkout main
git merge --no-ff hotfix/critical-bug
git push origin main

# 4. 合并回 develop
git checkout develop
git merge --no-ff hotfix/critical-bug
git push origin develop

# 5. 删除 hotfix 分支
git branch -d hotfix/critical-bug
```

### 12.2 代码冲突处理

```bash
# 1. 更新本地分支
git checkout feature/your-feature
git fetch origin
git rebase origin/develop

# 2. 解决冲突
# ... 手动解决冲突 ...

# 3. 继续变基
git add .
git rebase --continue

# 4. 强制推送（谨慎使用）
git push origin feature/your-feature --force-with-lease
```

---

## 13. 工具推荐

### 13.1 Git 工具

- **命令行**：git
- **VSCode 插件**：GitLens, Git Graph
- **GUI 工具**：GitHub Desktop, SourceTree

### 13.2 代码质量工具

- **AI 代码审查**：Claude Code CLI (`/code-review`)
- **代码检查**：ESLint, TypeScript
- **格式化**：Prettier

---

## 14. 变更记录

| 版本 | 日期 | 变更内容 | 作者 |
|------|------|----------|------|
| v1.0.0 | 2025-01-18 | 初始版本 | Claude Code |

---

## 15. 参考文档

- [Git Flow](https://nvie.com/posts/a-successful-git-branching-model/)
- [GitHub Flow](https://docs.github.com/en/get-started/quickstart/github-flow)
- [Semantic Versioning](https://semver.org/)
- [Conventional Commits](https://www.conventionalcommits.org/)
